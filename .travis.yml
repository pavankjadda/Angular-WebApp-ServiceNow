os: linux
dist: trusty

language: node_js
node_js:
  - "12"
  - "14"

branches:
  only:
    - master

install:
  - npm install

jobs:
  include:
    ## Skip Tests
     - stage: test
       script: echo "Skipping Tests.."
      
    ## Build Stage  ##
    - stage: Build App
      script:
      - ng build --prod

notifications:
  email:
    on_failure: never
    on_success: never
